# DITA-OT Configuration
ditaOtVersion=4.2.3

# Enable parallel execution for multi-language builds
org.gradle.parallel=true

# Enable Configuration Cache for faster builds
org.gradle.configuration-cache=true

# Gradle daemon
org.gradle.daemon=true
