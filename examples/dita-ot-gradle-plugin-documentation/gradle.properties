# Configuration Cache disabled due to Provider API compatibility with download plugin
org.gradle.configuration-cache=false

# Enable parallel execution
org.gradle.parallel=true

# Enable Gradle daemon
org.gradle.daemon=true

# DITA-OT path (override with -PditaOtPath=/path/to/dita-ot)
# ditaOtPath=/path/to/dita-ot
