# Enable Configuration Cache for faster builds (v2.2.0+)
# Configuration cache speeds up subsequent builds by 10-50%
# by skipping the configuration phase entirely
org.gradle.configuration-cache=true

# Optional: Enable parallel execution for even faster builds
org.gradle.parallel=true

# Optional: Enable build cache for task output caching
org.gradle.caching=true
